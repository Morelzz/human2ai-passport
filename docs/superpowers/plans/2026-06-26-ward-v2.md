# Ward v2 (content leak finder) Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Un utente loggato lancia Ward su una propria immagine generata da Semblic; Ward cerca le copie su tutto il web, le tagga (reputazione del dominio + filigrana presente/assente), nasconde quelle messe in whitelist e le mostra nella UI onesta. L'utente decide; Nemesis agisce solo su suo comando (fase 2).

**Architecture:** Ward smette di proteggere "la persona" (richiedeva consenso/KYC) e protegge "i file generati da noi" (asset di proprieta', nessun consenso da chiedere). Riusa la discovery Google Vision (reverse-image) ma interrogata con l'IMMAGINE GENERATA, non con le foto del volto. Il verdetto non e' biometrico (face-api) ma per IMMAGINE: tipo di match Vision (full/partial) + pHash vs l'originale + lettura della filigrana invisibile (conferma "e' nostra" + quale certificato). Riusa `scan_jobs`/`scan_matches`/`allowlist`/`evidence_records`/`nemesis_actions` gia' in DB.

**Tech Stack:** Next.js (App Router) + TypeScript, Supabase (Postgres + Storage), Google Cloud Vision (WEB_DETECTION, gia' live), `lib/watermark` + `lib/stegano` (filigrana invisibile esistente), `lib/ward/evidence/phash`, vitest.

---

## Decisioni bloccate (dal brainstorm con Morelz, 2026-06-26)

- **Niente KYC, niente face-scan, niente PimEyes.** Tool INTERNO, si usa senza onboarding biometrico.
- **Solo immagini generate dentro Semblic.** Nient'altro entra in Ward v2.
- **Ward TROVA, non giudica.** Nessuna etichetta automatica "furto"/"in licenza". Nessuna cifra in euro a schermo (rischio legale: niente "recuperabile EUR X"). I tag sono NEUTRI.
- **Tag = reputazione dominio** (giallo `exposed` = zona conosciuta tipo Instagram, rosso `obscure` = forum/pagine nascoste) **+ filigrana** presente/assente (confidenza, non verdetto).
- **Whitelist NASCONDE:** un risultato messo in whitelist sparisce dai risultati e vive solo nel tab "In whitelist". Lo fa l'azione "Segna sicuro", niente pulsante nuovo.
- **L'utente decide. Nemesis parte solo su suo comando** (fase 2: il peso legale resta sul titolare dei diritti, Semblic non accusa mai).
- **Sigil** (esterni, "carico una foto trovata -> e' nostra?") e l'**avatar inverso** (registro per stare fuori dal generativo) NON si toccano: flussi separati.

## Scope

Questo piano copre la **fase 1** (il finder + whitelist + UI). Fase 2 (Nemesis user-triggered, cattura prove, schermata "Dove appaio" della persona) = piano separato.

## File structure

- Create `lib/ward/reputation.ts` — classificatore reputazione dominio (puro). Responsabilita': host -> `exposed`|`obscure`.
- Create `lib/ward/whitelist.ts` — split dei risultati per whitelist (puro). Responsabilita': dato l'elenco allowlist, separa visibili da nascosti.
- Create `lib/ward/provenance.ts` — tag di provenienza dalla filigrana (puro wrapper sull'esito del decode). Responsabilita': esito decode -> `{ ours, certificate }`.
- Create `lib/ward/content-scan.ts` — orchestratore dello scan su una generazione (DI, niente gate consenso, match per immagine). Responsabilita': pipeline find+tag+store, salta i whitelisted.
- Create `app/api/ward/content-scan/route.ts` — POST { generationId }, ownership, lancia content-scan.
- Create `app/api/ward/whitelist/route.ts` — POST per "Segna sicuro" (scrive `allowlist`).
- Create `app/ward/content/...` (componente UI finder) — il mockup onesto reso reale.
- Modify (migrazione, GATED su "applica"): `scan_jobs` (+`kind`,+`generation_id`), `scan_matches` (+`reputation`,+`watermark_present`,+`certificate`), wiring `allowlist`.

---

## Task 1: Classificatore reputazione dominio (puro)

**Files:**
- Create: `lib/ward/reputation.ts`
- Test: `lib/ward/reputation.test.ts`

- [ ] **Step 1: Write the failing test**

```ts
import { describe, it, expect } from "vitest";
import { domainReputation, KNOWN_PLATFORMS } from "./reputation";

describe("domainReputation", () => {
  it("le piattaforme note e i loro sottodomini sono 'exposed'", () => {
    expect(domainReputation("instagram.com")).toBe("exposed");
    expect(domainReputation("www.instagram.com")).toBe("exposed");
    expect(domainReputation("m.facebook.com")).toBe("exposed");
    expect(domainReputation("x.com")).toBe("exposed");
  });
  it("tutto il resto e' 'obscure' (default prudente)", () => {
    expect(domainReputation("dark-forum.ru")).toBe("obscure");
    expect(domainReputation("random-site.xyz")).toBe("obscure");
  });
  it("host nullo o vuoto = 'obscure'", () => {
    expect(domainReputation(null)).toBe("obscure");
    expect(domainReputation("")).toBe("obscure");
  });
  it("non si fa ingannare dal nome piattaforma come sottostringa", () => {
    expect(domainReputation("instagram.com.evil.ru")).toBe("obscure");
    expect(domainReputation("notinstagram.com")).toBe("obscure");
  });
  it("KNOWN_PLATFORMS e' non vuoto e tutto lowercase", () => {
    expect(KNOWN_PLATFORMS.length).toBeGreaterThan(10);
    expect(KNOWN_PLATFORMS.every((d) => d === d.toLowerCase())).toBe(true);
  });
});
```

- [ ] **Step 2: Run test to verify it fails**

Run: `npx vitest run lib/ward/reputation.test.ts`
Expected: FAIL (modulo non esiste).

- [ ] **Step 3: Write minimal implementation**

```ts
// Reputazione del dominio per il semaforo di Ward: 'exposed' = piattaforma
// conosciuta/esposta dove la presenza e' normale (giallo), 'obscure' = tutto il
// resto, da guardare per primo (rosso). Euristica di PRIORITA', non un verdetto:
// la whitelist dell'utente la corregge. Puro, niente rete. Match per dominio
// ESATTO o suffisso di sottodominio (mai sottostringa, anti spoofing).
export type Reputation = "exposed" | "obscure";

export const KNOWN_PLATFORMS = [
  "instagram.com", "facebook.com", "x.com", "twitter.com", "tiktok.com",
  "youtube.com", "linkedin.com", "pinterest.com", "reddit.com", "tumblr.com",
  "threads.net", "snapchat.com", "twitch.tv", "vimeo.com", "flickr.com",
  "behance.net", "dribbble.com", "medium.com", "substack.com", "wikipedia.org",
];

export function domainReputation(host: string | null | undefined): Reputation {
  if (!host) return "obscure";
  const h = host.toLowerCase().replace(/^www\./, "");
  const known = KNOWN_PLATFORMS.some((d) => h === d || h.endsWith("." + d));
  return known ? "exposed" : "obscure";
}
```

- [ ] **Step 4: Run test to verify it passes**

Run: `npx vitest run lib/ward/reputation.test.ts`
Expected: PASS.

- [ ] **Step 5: Commit**

```bash
git add lib/ward/reputation.ts lib/ward/reputation.test.ts
git commit -m "Ward v2: classificatore reputazione dominio (semaforo finder)"
```

---

## Task 2: Split per whitelist (puro)

**Files:**
- Create: `lib/ward/whitelist.ts`
- Test: `lib/ward/whitelist.test.ts`

- [ ] **Step 1: Write the failing test**

```ts
import { describe, it, expect } from "vitest";
import { isWhitelisted, splitByWhitelist } from "./whitelist";

type Find = { sourceUrl: string; pageUrl: string | null; host: string | null };
const f = (host: string, sourceUrl: string, pageUrl: string | null = null): Find => ({ host, sourceUrl, pageUrl });

describe("isWhitelisted", () => {
  it("match per host (l'intero dominio e' sicuro)", () => {
    expect(isWhitelisted(f("instagram.com", "https://instagram.com/a.jpg"), [{ type: "host", value: "instagram.com" }])).toBe(true);
  });
  it("match per url esatto (solo quella occorrenza)", () => {
    const find = f("blog.com", "https://blog.com/x.jpg", "https://blog.com/post");
    expect(isWhitelisted(find, [{ type: "url", value: "https://blog.com/x.jpg" }])).toBe(true);
    expect(isWhitelisted(find, [{ type: "url", value: "https://blog.com/post" }])).toBe(true);
  });
  it("nessun match = non in whitelist", () => {
    expect(isWhitelisted(f("a.ru", "https://a.ru/1.jpg"), [{ type: "host", value: "instagram.com" }])).toBe(false);
  });
});

describe("splitByWhitelist", () => {
  it("separa visibili da nascosti (i whitelisted spariscono dai risultati)", () => {
    const finds = [f("instagram.com", "https://instagram.com/a.jpg"), f("dark.ru", "https://dark.ru/b.jpg")];
    const { visible, whitelisted } = splitByWhitelist(finds, [{ type: "host", value: "instagram.com" }]);
    expect(visible.map((x) => x.host)).toEqual(["dark.ru"]);
    expect(whitelisted.map((x) => x.host)).toEqual(["instagram.com"]);
  });
});
```

- [ ] **Step 2: Run test to verify it fails**

Run: `npx vitest run lib/ward/whitelist.test.ts`
Expected: FAIL (modulo non esiste).

- [ ] **Step 3: Write minimal implementation**

```ts
// Whitelist di Ward: un risultato messo in whitelist NON compare nei risultati,
// resta solo nel tab dedicato. Voci per HOST (tutto il dominio sicuro) o per URL
// (solo quella occorrenza; una nuova ripubblicazione = nuovo url = ricompare).
// Puro: lavora su qualsiasi oggetto con host/sourceUrl/pageUrl.
export type AllowEntry = { type: "host" | "url"; value: string };
type HasUrls = { sourceUrl: string; pageUrl: string | null; host: string | null };

export function isWhitelisted(find: HasUrls, entries: AllowEntry[]): boolean {
  const host = (find.host ?? "").toLowerCase().replace(/^www\./, "");
  return entries.some((e) => {
    if (e.type === "host") {
      const v = e.value.toLowerCase().replace(/^www\./, "");
      return host === v || host.endsWith("." + v);
    }
    return find.sourceUrl === e.value || find.pageUrl === e.value;
  });
}

export function splitByWhitelist<T extends HasUrls>(finds: T[], entries: AllowEntry[]): { visible: T[]; whitelisted: T[] } {
  const visible: T[] = [];
  const whitelisted: T[] = [];
  for (const f of finds) (isWhitelisted(f, entries) ? whitelisted : visible).push(f);
  return { visible, whitelisted };
}
```

- [ ] **Step 4: Run test to verify it passes**

Run: `npx vitest run lib/ward/whitelist.test.ts`
Expected: PASS.

- [ ] **Step 5: Commit**

```bash
git add lib/ward/whitelist.ts lib/ward/whitelist.test.ts
git commit -m "Ward v2: whitelist che nasconde i risultati certificati sicuri"
```

---

## Task 3: Tag di provenienza dalla filigrana (puro wrapper)

**Files:**
- Create: `lib/ward/provenance.ts`
- Test: `lib/ward/provenance.test.ts`
- Read first: `lib/watermark.ts`, `lib/stegano.ts` (per la forma reale dell'esito del decode; il wrapper qui sotto si adegua a quella forma).

- [ ] **Step 1: Write the failing test**

```ts
import { describe, it, expect } from "vitest";
import { provenanceTag } from "./provenance";

describe("provenanceTag", () => {
  it("filigrana decodificata a un certificato = nostra", () => {
    expect(provenanceTag("a7f3d29b")).toEqual({ ours: true, certificate: "a7f3d29b" });
  });
  it("niente filigrana = non confermata (ma non e' un verdetto di estraneita')", () => {
    expect(provenanceTag(null)).toEqual({ ours: false, certificate: null });
    expect(provenanceTag("")).toEqual({ ours: false, certificate: null });
  });
});
```

- [ ] **Step 2: Run test to verify it fails**

Run: `npx vitest run lib/ward/provenance.test.ts`
Expected: FAIL.

- [ ] **Step 3: Write minimal implementation**

```ts
// Tag di provenienza: trasforma l'esito del decode della filigrana (il
// certificato estratto, o null se illeggibile) in un tag NEUTRO. "ours: true"
// significa "e' quasi certo nostra, certificato X"; "false" significa solo "non
// confermata dalla filigrana" (puo' essere una copia editata che l'ha persa,
// non un giudizio sull'autore). La decodifica vera la fa lib/watermark a monte.
export function provenanceTag(certificate: string | null): { ours: boolean; certificate: string | null } {
  const c = (certificate ?? "").trim();
  return c ? { ours: true, certificate: c } : { ours: false, certificate: null };
}
```

- [ ] **Step 4: Run test to verify it passes**

Run: `npx vitest run lib/ward/provenance.test.ts`
Expected: PASS.

- [ ] **Step 5: Commit**

```bash
git add lib/ward/provenance.ts lib/ward/provenance.test.ts
git commit -m "Ward v2: tag di provenienza dalla filigrana (neutro)"
```

---

## Task 4: Migrazione DB (GATED su "applica")

**Files:**
- Migrazione `ward_v2_content_scan`.

NON applicare senza il trigger esplicito "applica" di Morelz.

- [x] **Step 1:** Letto lo schema reale (`list_tables` 2026-06-26). Note: `scan_jobs.avatar_id` e `scan_matches.avatar_id` sono **NOT NULL** (FK avatars), `allowlist` = `avatar_id`(NOT NULL)+`host`(NOT NULL)+`reason` con **0 righe** e **non usata da runScan**. `generations` ha `avatar_id`+`buyer_id`+`image_url`+`certificate`. `scan_matches.band` ha gia' check (confirmed|review), `sensitivity` default 'standard'.
- [x] **Step 2:** SQL rifinita (sotto). Scelte: `avatar_id` resta NOT NULL ovunque, si passa quello della GENERAZIONE (niente nullable-migration, integrita' FK + utile alla vista-persona di fase 2). `allowlist.host` resta NOT NULL (per le url scrivo comunque l'host estratto in `host`, l'url completo in `value`).
- [x] **Step 3:** APPLICATA 2026-06-27 (su "applica tutto" di Morelz), nome `ward_v2_content_scan`, `success`. Verificate le 8 colonne (information_schema) + selftest transazionale (insert reali del repo poi cleanup) = OK. Advisor: nessuna falla nuova (solo i 2 indici nuovi come `unused_index` INFO, attesi). NB: gli advisor mostrano molti `auth_rls_initplan` WARN preesistenti (NON da questa migrazione, che e' solo colonne+indici): da rivedere a parte.

**SQL FINALE (pronta per "applica"):**

```sql
-- Ward v2 content leak finder: riusa scan_jobs/scan_matches/allowlist.
-- scan_jobs: distingue il tipo di scan e lega alla generazione. avatar_id resta
-- NOT NULL = l'avatar della generazione (integrita' FK + vista persona fase 2).
alter table scan_jobs add column if not exists kind text not null default 'person';
alter table scan_jobs add column if not exists generation_id uuid references generations(id) on delete cascade;

-- scan_matches: tag NEUTRI del finder (reputazione dominio + filigrana). Nessun
-- punteggio/accusa: band(confirmed|review) gia' c'e'.
alter table scan_matches add column if not exists reputation text;          -- 'exposed' | 'obscure'
alter table scan_matches add column if not exists watermark_present boolean; -- filigrana invisibile letta
alter table scan_matches add column if not exists certificate text;         -- certificato decodificato (se nostra)

-- allowlist: estesa a { generation_id, entry_type('host'|'url'), value }. host
-- resta NOT NULL (per le url si scrive comunque l'host estratto); value tiene
-- l'url completo per entry_type='url'.
alter table allowlist add column if not exists generation_id uuid references generations(id) on delete cascade;
alter table allowlist add column if not exists entry_type text not null default 'host' check (entry_type in ('host','url'));
alter table allowlist add column if not exists value text;

create index if not exists idx_scan_jobs_generation on scan_jobs(generation_id);
create index if not exists idx_allowlist_generation on allowlist(generation_id);
```

NB: l'idempotenza del "Segna sicuro" (Task 7) e' gestita a livello applicativo (select-then-insert), non con unique-index (per evitare nulli in chiave). Le 11 righe `scan_jobs` esistenti diventano `kind='person'`, le 21 `scan_matches` hanno i tag nuovi = NULL: nessuna rottura del person-scan.

---

## Task 5: Orchestratore `runContentScan` (DI, niente gate consenso)

**Files:**
- Create: `lib/ward/content-scan.ts`
- Test: `lib/ward/content-scan.test.ts`
- Read first: `lib/ward/scan.ts` (modello DI da imitare), `lib/ward/discovery` (firma `find`), `lib/ward/evidence/phash`.

Differenze chiave da `runScan` (da implementare con fake iniettati, come scan.test.ts):
- **Nessun gate consenso** (l'asset e' nostro): si parte diretti al job.
- **Query discovery = i bytes della GENERAZIONE** (una sola immagine, non gli angoli del volto).
- **Verdetto per immagine, non biometrico:** `band='confirmed'` se Vision da full match O se pHash(candidate) entro soglia da pHash(originale); `band='review'` per partial/visually-similar; scarta il resto.
- **Tag per match:** `reputation = domainReputation(host)` (Task 1); `watermark_present` + `certificate` dal decode della filigrana sul candidato (Task 3); `phash`.
- **Whitelist:** caricare le voci `allowlist` dell'utente/generazione e SALTARE i candidati gia' whitelisted (Task 2) prima di salvarli.
- Riusa `insertCandidate`/`deleteCandidate` (transitorieta', A2.3) e `finishJob`.

- [x] Step 0 (abilitante): la discovery ora porta il `matchKind` (full/partial/similar) per candidato, serviva al verdetto. `Candidate.matchKind?` in `lib/ward/discovery/types.ts`, popolato in `parseWebDetection` (full > partial > similar, primo vince sul dedup), re-export in `discovery/index.ts`. Commit `b75993b`.
- [x] Step 1: test con fake discovery/fetch/phash/decoder che verifica: confirmed via full-match salvato con reputation+watermark; confirmed via phash vicino; review via partial/similar; discard; whitelisted SALTATO (mai inserito); candidato sempre cancellato. + unit test puri `hammingHex` e `contentVerdict`.
- [x] Step 2-4: TDD fino al verde (11 test verdi; suite ward 127/127; tsc 0).
- [x] Step 5: commit `2ce11a5` `Ward v2: runContentScan (scan di una generazione, no consenso, match per immagine)`.

**FATTO (commit locali, NON pushati):** `lib/ward/content-scan.ts` con `runContentScan(generationId, deps)` (DI), `hammingHex`, `contentVerdict`, tipi `ContentMatchRow`/`ContentScanRepository`/`ContentScanDeps`/`ContentScanResult`. Verdetto: `full` di Vision O phash entro 8 bit dall'originale = confirmed; `partial`/`similar` = review; resto discard. Tag: `domainReputation(host)` + filigrana via `extractStego` (lazy) -> `provenanceTag`. Whitelist via `isWhitelisted` SALTA prima di inserire. Default lazy: audit `./audit`, phash `./evidence/phash`, decoder `../stegano`, provider `./discovery`. **PROSSIMO:** Task 4 (migrazione, GATED "applica") oppure Task 6 (route POST /api/ward/content-scan) che carica i bytes della generazione e chiama `runContentScan`.

---

## Task 6: Route `POST /api/ward/content-scan`

**Files:**
- Create: `app/api/ward/content-scan/route.ts`
- Read first: `app/api/ward/scan/route.ts` (auth + ownership + rate-limit da imitare), come si carica l'immagine generata (bucket `generations` / `/api/content/[cert]`).

Logica: auth; rate-limit `ward-content-scan:<user>`; body `{ generationId }`; ownership della generazione (l'utente la possiede); carica i bytes dell'immagine generata; `runContentScan(generationId, { referenceImageBytes, ... })`; ritorna i risultati VISIBILI (whitelisted gia' esclusi). Niente consenso.

- [ ] TDD/verifica a terra (route 401 senza auth sul dev), poi commit.

---

## Task 7: Route `POST /api/ward/whitelist` ("Segna sicuro")

**Files:**
- Create: `app/api/ward/whitelist/route.ts`

Logica: auth + ownership; body `{ matchId, scope: 'url'|'host' }`; scrive una voce in `allowlist` (per url della occorrenza, o per host dell'intero dominio) legata a owner+generazione; risponde ok. Idempotente.

- [ ] TDD/verifica, commit.

---

## Task 8: UI finder onesta

**Files:**
- Create: componente client sotto `app/ward/` (sorgente del look: il mockup show_widget `ward_v2_honest_finder_mockup`, design SEMBLIC obsidian/amber).
- Read first: `app/ward/WardApp.tsx` (shell e tab esistenti) per agganciarsi.

Resa: per una generazione, lista delle copie VISIBILI con: pallino reputazione (giallo `exposed` / rosso `obscure`), tag filigrana presente/assente, azioni "Apri" / "Segna sicuro" (chiama Task 7, l'item sparisce) / "Avvia rimozione" (placeholder fase 2). Tab "Tutte / Da rivedere / In whitelist". Nessuna cifra, nessuna accusa automatica.

- [ ] Costruire, verificare a terra (preview), commit.

---

## Self-review

- **Spec coverage:** niente-KYC (Task 5/6 nessun gate), solo-generazioni (Task 6 ownership su `generations`), trova-non-giudica (tag neutri Task 1/3, niente euro), whitelist-nasconde (Task 2 + 7 + 8), semaforo dominio (Task 1 + 8), Nemesis su comando (Task 8 placeholder, vero in fase 2). Coperto.
- **Placeholder:** Task 1-3 hanno codice completo. Task 4-8 dipendono da codice ancora da leggere (watermark/generation/discovery): ogni task ha "Read first" come primo passo, e la firma finale si chiude contro il codice reale a esecuzione. La migrazione e' gated su "applica".
- **Type consistency:** `Reputation`('exposed'|'obscure'), `AllowEntry`{type,value}, `provenanceTag`->{ours,certificate} usati coerenti tra Task 1/2/3 e referenziati in Task 5/8.

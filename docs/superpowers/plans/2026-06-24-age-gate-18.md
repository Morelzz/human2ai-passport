# Age-gate 18+ (CRIT-10) Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Impedire che minori usino SEMBLIC e, soprattutto, che un volto di minore entri nel registro, con un gate 18+ a strati (autodichiarazione al signup + verifica documentale via Didit per gli avatar).

**Architecture:** Logica d'età in funzioni pure testabili (`lib/age.ts`, `lib/adult-gate.ts`). Enforcement fail-closed in tre punti: client UX al signup, trigger DB autoritativo, e gate a valle su ogni azione sensibile (generazione, creazione avatar). Lo strato forte vive nel webhook Didit: estrae la data di nascita dal documento e rifiuta gli under-18 alla radice. Dati minimizzati su `profiles` (mai salvata la DOB del documento, solo l'esito).

**Tech Stack:** Next.js (App Router) + TypeScript, Supabase (Postgres + trigger plpgsql), Vitest (test in `lib/**/*.test.ts`).

**Spec di riferimento:** `docs/superpowers/specs/2026-06-24-age-gate-18-design.md`

**Branch:** `age-gate-18` (già creato, contiene il commit dello spec).

**Comandi di verifica del progetto:**
- Test: `npm test` (alias di `vitest run`)
- Type-check: `node node_modules/typescript/bin/tsc --noEmit`
- Build: `npm run build`

**Rituale git/DB:** commit in italiano senza trattini lunghi, trailer `Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>`. `apply_migration` e `git push` solo su parola esplicita di Morelz ("applica" / "pubblica").

---

## File Structure

**Creare:**
- `lib/age.ts` — calcolo età puro (`ageFromDob`, `isAdult`, `kycAdultOutcome`).
- `lib/age.test.ts` — test del calcolo età.
- `lib/adult-gate.ts` — gate puro (`adultGateReason`).
- `lib/adult-gate.test.ts` — test del gate.
- `supabase/age_gate_profiles.sql` — migrazione: 3 colonne + check + trigger esteso.
- `app/api/account/confirm-age/route.ts` — conferma DOB una-tantum (utenti esistenti).
- `app/match/AgeGateModal.tsx` — modale prompt DOB nella UI di generazione.

**Modificare:**
- `lib/kyc/didit.ts` — `extractDobFromDecision` (puro) + `getDiditDob` (fetch).
- `lib/kyc/didit.test.ts` — test di `extractDobFromDecision`.
- `lib/observability.ts` — nuova `DegradationArea` `"kyc.dob_missing"`.
- `app/api/kyc/didit/webhook/route.ts` — strato forte (under-18 rifiutato, adulto documentale).
- `app/api/generate/route.ts` — gate età sul chiamante.
- `app/signup/SignupForm.tsx` — campo DOB + schermata di blocco.
- `app/api/avatar/create/route.ts` — richiede adulto documentale per il volto nel registro.

**Decisioni con dipendenza esterna (gestite in task dedicati):**
- Applicazione migrazione → parola "applica" di Morelz (Task 3).
- Lista founder per il back-fill → conferma di Morelz (Task 11).
- Nome esatto del campo DOB nella decisione Didit → da confermare su decisione reale (Task 4, già gestito fail-closed).

---

## Task 1: Funzioni pure di età (`lib/age.ts`)

**Files:**
- Create: `lib/age.ts`
- Test: `lib/age.test.ts`

- [ ] **Step 1: Scrivi il test che fallisce**

```typescript
// lib/age.test.ts
import { describe, it, expect } from "vitest";
import { ageFromDob, isAdult, kycAdultOutcome } from "./age";

// Riferimento temporale fisso: 24 giugno 2026 (ora locale).
const NOW = new Date(2026, 5, 24);

describe("ageFromDob", () => {
  it("compie 18 esattamente oggi: 18", () => {
    expect(ageFromDob("2008-06-24", NOW)).toBe(18);
  });
  it("li compie domani: ancora 17", () => {
    expect(ageFromDob("2008-06-25", NOW)).toBe(17);
  });
  it("li ha compiuti ieri: 18", () => {
    expect(ageFromDob("2008-06-23", NOW)).toBe(18);
  });
  it("nato il 29 febbraio (bisestile): conta gli anni pieni", () => {
    expect(ageFromDob("2000-02-29", NOW)).toBe(26);
  });
  it("data futura: null", () => {
    expect(ageFromDob("2030-01-01", NOW)).toBeNull();
  });
  it("età assurda (> 120): null", () => {
    expect(ageFromDob("1800-01-01", NOW)).toBeNull();
  });
  it("formato non valido: null", () => {
    expect(ageFromDob("non-una-data", NOW)).toBeNull();
  });
  it("data inesistente (30 febbraio): null", () => {
    expect(ageFromDob("2026-02-30", NOW)).toBeNull();
  });
});

describe("isAdult", () => {
  it("18 oggi: adulto", () => expect(isAdult("2008-06-24", NOW)).toBe(true));
  it("17: non adulto", () => expect(isAdult("2008-06-25", NOW)).toBe(false));
  it("formato non valido: non adulto (fail-closed)", () => expect(isAdult("xxx", NOW)).toBe(false));
});

describe("kycAdultOutcome", () => {
  it("dob assente: unknown", () => expect(kycAdultOutcome(null, NOW)).toBe("unknown"));
  it("adulto: ok", () => expect(kycAdultOutcome("2000-01-01", NOW)).toBe("ok"));
  it("minore: under_18", () => expect(kycAdultOutcome("2012-01-01", NOW)).toBe("under_18"));
  it("dob illeggibile: unknown", () => expect(kycAdultOutcome("xxx", NOW)).toBe("unknown"));
});
```

- [ ] **Step 2: Esegui il test, verifica che fallisca**

Run: `npm test -- lib/age.test.ts`
Expected: FAIL (`Cannot find module './age'`).

- [ ] **Step 3: Implementa `lib/age.ts`**

```typescript
// lib/age.ts
// Calcolo età puro e fail-closed per l'age-gate 18+. Nessun IO, nessuna
// dipendenza: tutto testabile. Confronto fatto su componenti di data (anno/
// mese/giorno) per evitare slittamenti di fuso. La data di nascita arriva
// sempre come "YYYY-MM-DD".

// Anni pieni tra dob e now, oppure null se la data e' malformata, inesistente,
// futura o assurda (> 120 anni). Il null e' volutamente "non verificabile".
export function ageFromDob(dob: string, now: Date): number | null {
  const m = /^(\d{4})-(\d{2})-(\d{2})$/.exec(dob);
  if (!m) return null;
  const y = Number(m[1]);
  const mo = Number(m[2]);
  const d = Number(m[3]);
  // La data deve esistere davvero (scarta 2026-02-30, mese 13, ecc.).
  const probe = new Date(y, mo - 1, d);
  if (probe.getFullYear() !== y || probe.getMonth() !== mo - 1 || probe.getDate() !== d) return null;
  const ny = now.getFullYear();
  const nmo = now.getMonth() + 1;
  const nd = now.getDate();
  // Data futura: non valida.
  if (y > ny || (y === ny && (mo > nmo || (mo === nmo && d > nd)))) return null;
  let age = ny - y;
  if (nmo < mo || (nmo === mo && nd < d)) age -= 1;
  if (age > 120) return null;
  return age;
}

// Maggiorenne? Fail-closed: qualunque input non verificabile vale false.
export function isAdult(dob: string, now: Date): boolean {
  const age = ageFromDob(dob, now);
  return age !== null && age >= 18;
}

// Esito d'eta per lo strato forte (documento Didit): "unknown" quando la data
// non e' leggibile, cosi' il chiamante puo' distinguere "minore" da "non so".
export function kycAdultOutcome(dob: string | null, now: Date): "ok" | "under_18" | "unknown" {
  if (!dob) return "unknown";
  const age = ageFromDob(dob, now);
  if (age === null) return "unknown";
  return age >= 18 ? "ok" : "under_18";
}
```

- [ ] **Step 4: Esegui il test, verifica che passi**

Run: `npm test -- lib/age.test.ts`
Expected: PASS (tutti i casi verdi).

- [ ] **Step 5: Commit**

```bash
git add lib/age.ts lib/age.test.ts
git commit -m "Age-gate: funzioni pure di calcolo eta (lib/age)

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>"
```

---

## Task 2: Gate adulto puro (`lib/adult-gate.ts`)

**Files:**
- Create: `lib/adult-gate.ts`
- Test: `lib/adult-gate.test.ts`

Segue il pattern di `lib/avatar-gate.ts` e `lib/consent-gate.ts`: funzione pura, esito loggabile, assenza di dato = blocco.

- [ ] **Step 1: Scrivi il test che fallisce**

```typescript
// lib/adult-gate.test.ts
import { describe, it, expect } from "vitest";
import { adultGateReason } from "./adult-gate";

const NOW = new Date(2026, 5, 24);

describe("adultGateReason", () => {
  it("profilo assente: no_dob", () => {
    expect(adultGateReason(null, NOW)).toBe("no_dob");
  });
  it("account pre-gate (nessuna data, mai verificato): no_dob", () => {
    expect(adultGateReason({ date_of_birth: null, adult_verified_at: null }, NOW)).toBe("no_dob");
  });
  it("verificato da documento (data non salvata ma esito presente): via libera", () => {
    expect(adultGateReason({ date_of_birth: null, adult_verified_at: "2026-06-23T10:00:00Z" }, NOW)).toBeNull();
  });
  it("autodichiarato adulto: via libera", () => {
    expect(adultGateReason({ date_of_birth: "2000-01-01", adult_verified_at: "2026-06-23T10:00:00Z" }, NOW)).toBeNull();
  });
  it("data presente ma minorenne (difesa): under_18", () => {
    expect(adultGateReason({ date_of_birth: "2012-01-01", adult_verified_at: null }, NOW)).toBe("under_18");
  });
  it("data presente adulta senza esito: via libera", () => {
    expect(adultGateReason({ date_of_birth: "1990-01-01", adult_verified_at: null }, NOW)).toBeNull();
  });
});
```

- [ ] **Step 2: Esegui il test, verifica che fallisca**

Run: `npm test -- lib/adult-gate.test.ts`
Expected: FAIL (`Cannot find module './adult-gate'`).

- [ ] **Step 3: Implementa `lib/adult-gate.ts`**

```typescript
// lib/adult-gate.ts
// Gate 18+ a valle: chi compie un'azione sensibile (generazione, ecc.) deve
// risultare adulto verificato. Funzione PURA, fail-closed. Si appoggia
// principalmente a adult_verified_at (valorizzato sia dall'autodichiarazione al
// signup, metodo 'self', sia dall'approvazione Didit, metodo 'document'); la
// data di nascita resta come controllo difensivo.

import { isAdult } from "./age";

export interface AdultGateState {
  date_of_birth: string | null;
  adult_verified_at: string | null;
}

export type AdultGateReason = "no_dob" | "under_18";

// null = via libera. "no_dob" = account pre-gate da far passare dal prompt
// una-tantum. "under_18" = minorenne, blocco secco.
export function adultGateReason(profile: AdultGateState | null, now: Date): AdultGateReason | null {
  if (!profile) return "no_dob";
  if (profile.adult_verified_at) return null;
  if (!profile.date_of_birth) return "no_dob";
  return isAdult(profile.date_of_birth, now) ? null : "under_18";
}
```

- [ ] **Step 4: Esegui il test, verifica che passi**

Run: `npm test -- lib/adult-gate.test.ts`
Expected: PASS.

- [ ] **Step 5: Commit**

```bash
git add lib/adult-gate.ts lib/adult-gate.test.ts
git commit -m "Age-gate: funzione pura adultGateReason (gate a valle)

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>"
```

---

## Task 3: Migrazione DB (colonne + check + trigger)

**Files:**
- Create: `supabase/age_gate_profiles.sql`

Questa migrazione aggiunge le 3 colonne ed estende `handle_new_user` per scrivere la DOB autodichiarata e bloccare gli under-18 dichiarati. NB: GoTrue riporta l'eccezione del trigger al client come errore generico, quindi la schermata di blocco la guida il check client-side (Task 7); il trigger e' la garanzia autoritativa dietro.

- [ ] **Step 1: Scrivi il file di migrazione**

```sql
-- supabase/age_gate_profiles.sql
-- Age-gate 18+ (CRIT-10). Tre colonne su profiles (minimizzate) + estensione
-- del trigger handle_new_user per l'autodichiarazione al signup.

alter table profiles
  add column if not exists date_of_birth date,
  add column if not exists adult_verified_at timestamptz,
  add column if not exists adult_verified_method text;

do $$
begin
  if not exists (
    select 1 from pg_constraint where conname = 'profiles_adult_method_chk'
  ) then
    alter table profiles
      add constraint profiles_adult_method_chk
      check (adult_verified_method is null or adult_verified_method in ('self','document'));
  end if;
end $$;

-- Estende il trigger esistente: legge date_of_birth dal raw_user_meta_data
-- (options.data di signUp). Se presente e under-18 -> raise (account non creato).
-- Se presente e >= 18 -> scrive data + esito 'self'. Se assente -> nulla (il
-- gate a valle col prompt una-tantum gestira' l'account senza data).
create or replace function handle_new_user()
returns trigger
language plpgsql
security definer
set search_path = public
as $$
declare
  v_role text;
  v_name text;
  v_dob_text text;
  v_dob date;
  v_age int;
begin
  v_role := case when new.raw_user_meta_data->>'role' = 'seller' then 'seller' else 'buyer' end;
  v_name := nullif(btrim(new.raw_user_meta_data->>'full_name'), '');

  v_dob_text := nullif(btrim(new.raw_user_meta_data->>'date_of_birth'), '');
  if v_dob_text is not null then
    begin
      v_dob := v_dob_text::date;
    exception when others then
      v_dob := null; -- formato non valido: trattato come assente (gate a valle)
    end;
  end if;

  if v_dob is not null then
    if v_dob > current_date then
      raise exception 'age_gate: data di nascita futura';
    end if;
    v_age := date_part('year', age(current_date, v_dob));
    if v_age < 18 then
      raise exception 'age_gate: under_18';
    end if;
  end if;

  insert into public.profiles (id, email, full_name, role, date_of_birth, adult_verified_at, adult_verified_method)
  values (
    new.id, new.email, v_name, v_role,
    v_dob,
    case when v_dob is not null then now() else null end,
    case when v_dob is not null then 'self' else null end
  )
  on conflict (id) do nothing;
  return new;
end;
$$;
```

- [ ] **Step 2: Applica la migrazione (gated: serve "applica" di Morelz)**

Mostra l'SQL a Morelz. Su parola "applica", esegui via connettore Supabase:
`apply_migration` con name `age_gate_profiles` e il corpo del file.

- [ ] **Step 3: Verifica lo schema a terra**

Esegui via `execute_sql`:
```sql
select column_name, data_type from information_schema.columns
where table_name = 'profiles' and column_name in ('date_of_birth','adult_verified_at','adult_verified_method')
order by column_name;
```
Expected: 3 righe (date, timestamptz, text).

Verifica il trigger con un giro a secco in transazione (rollback, non sporca i dati):
```sql
begin;
-- under-18 dichiarato: deve sollevare 'age_gate: under_18'
do $$ begin
  perform handle_new_user_test_helper(); -- placeholder concettuale: vedi nota sotto
end $$;
rollback;
```
Nota: il trigger non e' chiamabile a mano in modo semplice (gira su auth.users). La verifica reale del raise avviene in Task 7 (signup) e con un signup di prova under-18 in ambiente di test. Qui basta confermare colonne + che `handle_new_user` esista aggiornata:
```sql
select pg_get_functiondef('handle_new_user'::regproc) ilike '%age_gate: under_18%' as has_guard;
```
Expected: `has_guard = true`.

- [ ] **Step 4: Commit del file di migrazione**

```bash
git add supabase/age_gate_profiles.sql
git commit -m "Age-gate: migrazione profiles (3 colonne) + trigger signup con guardia 18+

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>"
```

---

## Task 4: Estrazione DOB dalla decisione Didit (`lib/kyc/didit.ts`)

**Files:**
- Modify: `lib/kyc/didit.ts` (aggiungi `extractDobFromDecision` + `getDiditDob`)
- Test: `lib/kyc/didit.test.ts` (aggiungi i test dell'estrattore puro)

Il nome del campo (`date_of_birth`) e' presunto sullo stesso `decision.id_verifications[]` da cui gia' leggiamo `portrait_image`; va confermato su una decisione reale (console Didit "Try Webhook" o sandbox). Se assente, vale il ramo fail-closed nel webhook (Task 5).

- [ ] **Step 1: Scrivi i test che falliscono (in coda a `lib/kyc/didit.test.ts`)**

```typescript
// in cima al file, aggiungi all'import esistente:
//   import { verifyDiditWebhook, diditStatusToKyc, extractDobFromDecision } from "./didit";

describe("extractDobFromDecision", () => {
  it("legge la data dal primo id_verification (radice)", () => {
    const data = { id_verifications: [{ date_of_birth: "1990-05-10", portrait_image: "x" }] };
    expect(extractDobFromDecision(data)).toBe("1990-05-10");
  });
  it("legge la data quando id_verifications e' annidato sotto decision", () => {
    const data = { decision: { id_verifications: [{ date_of_birth: "2001-12-01" }] } };
    expect(extractDobFromDecision(data)).toBe("2001-12-01");
  });
  it("null se manca la data", () => {
    expect(extractDobFromDecision({ id_verifications: [{ portrait_image: "x" }] })).toBeNull();
  });
  it("null se il formato e' sbagliato", () => {
    expect(extractDobFromDecision({ id_verifications: [{ date_of_birth: "10/05/1990" }] })).toBeNull();
  });
  it("null su payload vuoto o non-oggetto", () => {
    expect(extractDobFromDecision(null)).toBeNull();
    expect(extractDobFromDecision("x")).toBeNull();
  });
});
```

- [ ] **Step 2: Esegui i test, verifica che falliscano**

Run: `npm test -- lib/kyc/didit.test.ts`
Expected: FAIL (`extractDobFromDecision is not exported`).

- [ ] **Step 3: Implementa in `lib/kyc/didit.ts`**

Aggiungi, dopo `getDiditPortraitUrl` (circa riga 93):

```typescript
// Estrae la data di nascita (YYYY-MM-DD) dalla decisione Didit, dallo stesso
// array id_verifications[] da cui leggiamo il portrait. PURA e testabile.
// null se assente o malformata: il chiamante (webhook) la tratta fail-closed.
export function extractDobFromDecision(data: unknown): string | null {
  if (!data || typeof data !== "object") return null;
  const root = data as Record<string, unknown>;
  const decision = (root.decision as Record<string, unknown> | undefined) ?? root;
  const idv = Array.isArray(decision.id_verifications)
    ? (decision.id_verifications as Record<string, unknown>[])
    : [];
  for (const v of idv) {
    const dob = v.date_of_birth;
    if (typeof dob === "string" && /^\d{4}-\d{2}-\d{2}$/.test(dob)) return dob;
  }
  return null;
}

// Recupera la data di nascita verificata dal documento per una sessione. null se
// non disponibile (sessione non trovata, decisione senza data, ecc.).
export async function getDiditDob(sessionId: string): Promise<string | null> {
  const apiKey = process.env.DIDIT_API_KEY;
  if (!apiKey || !sessionId) return null;
  const res = await fetch(`${DIDIT_BASE_URL}/v3/session/${encodeURIComponent(sessionId)}/decision/`, {
    headers: { "X-API-Key": apiKey },
  });
  if (!res.ok) return null;
  const data = (await res.json().catch(() => null)) as unknown;
  return extractDobFromDecision(data);
}
```

- [ ] **Step 4: Esegui i test, verifica che passino**

Run: `npm test -- lib/kyc/didit.test.ts`
Expected: PASS (vecchi test firma + nuovi test estrattore).

- [ ] **Step 5: Commit**

```bash
git add lib/kyc/didit.ts lib/kyc/didit.test.ts
git commit -m "Age-gate: estrazione data di nascita dalla decisione Didit

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>"
```

---

## Task 5: Strato forte nel webhook Didit

**Files:**
- Modify: `lib/observability.ts:10-16` (aggiungi area di degrado)
- Modify: `app/api/kyc/didit/webhook/route.ts`

- [ ] **Step 1: Aggiungi la `DegradationArea` per la DOB illeggibile**

In `lib/observability.ts`, estendi l'unione (riga ~10-16):

```typescript
export type DegradationArea =
  | "kyc.stub_used"
  | "kyc.didit_unconfigured"
  | "kyc.dob_missing"
  | "identity_gate.models_unavailable"
  | "veto.scan_unavailable"
  | "ward.discovery_stub"
  | "ratelimit.unavailable";
```

- [ ] **Step 2: Type-check (nessun test unitario per la route: testiamo le parti pure altrove)**

Run: `node node_modules/typescript/bin/tsc --noEmit`
Expected: PASS.

- [ ] **Step 3: Modifica il webhook**

In `app/api/kyc/didit/webhook/route.ts` aggiorna gli import in cima:

```typescript
import { NextResponse } from "next/server";
import { createServerClient } from "@/lib/supabase";
import { verifyDiditWebhook, diditStatusToKyc, getDiditDob } from "@/lib/kyc/didit";
import { kycAdultOutcome } from "@/lib/age";
import { reportDegradation } from "@/lib/observability";
import { appendAudit } from "@/lib/ward/audit";
```

Sostituisci il blocco `if (userId && kyc) { ... }` (righe ~38-62) con:

```typescript
  if (userId && kyc) {
    const admin = createServerClient();

    // Strato forte 18+: all'approvazione, leggiamo la data di nascita dal
    // documento e decidiamo. Non salviamo MAI la DOB documentale: solo l'esito.
    let finalKyc: "approved" | "rejected" | "pending" = kyc;
    const adultPatch: Record<string, unknown> = {};
    let underageBlocked = false;

    if (kyc === "approved") {
      const dob = await getDiditDob(payload.session_id);
      const outcome = kycAdultOutcome(dob, new Date());
      if (outcome === "under_18") {
        // Minore col documento: nessun volto di minore entra nel registro.
        finalKyc = "rejected";
        underageBlocked = true;
        console.error(`[SEMBLIC:SICUREZZA] kyc.underage_blocked user=${userId} session=${payload.session_id}`);
      } else if (outcome === "ok") {
        adultPatch.adult_verified_at = new Date().toISOString();
        adultPatch.adult_verified_method = "document";
      } else {
        // DOB illeggibile: niente stato document-adult, revisione manuale.
        reportDegradation("kyc.dob_missing", { user: userId, session: payload.session_id });
      }
    }

    await admin
      .from("profiles")
      .update({ kyc_status: finalKyc, kyc_provider: "didit", identity_session_id: payload.session_id, ...adultPatch })
      .eq("id", userId);

    await appendAudit({
      actor: userId,
      action: underageBlocked ? "kyc.underage_blocked" : "kyc.didit_webhook",
      target: userId,
      meta: {
        provider: "didit",
        status: payload.status,
        kyc: finalKyc,
        adult_method: adultPatch.adult_verified_method ?? null,
        session_id: payload.session_id,
        event_id: payload.event_id ?? null,
        environment: payload.environment ?? null,
      },
    });
  }
```

- [ ] **Step 4: Type-check + suite completa**

Run: `node node_modules/typescript/bin/tsc --noEmit && npm test`
Expected: PASS (nessuna regressione).

- [ ] **Step 5: Commit**

```bash
git add lib/observability.ts app/api/kyc/didit/webhook/route.ts
git commit -m "Age-gate: strato forte nel webhook Didit (under-18 rifiutato, adulto documentale)

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>"
```

---

## Task 6: Gate età sulla generazione (`app/api/generate/route.ts`)

**Files:**
- Modify: `app/api/generate/route.ts`

- [ ] **Step 1: Aggiungi l'import**

In cima al file aggiungi:
```typescript
import { adultGateReason } from "@/lib/adult-gate";
```

- [ ] **Step 2: Inserisci il gate dopo la creazione di `admin` (riga ~69), prima del fetch dell'avatar**

```typescript
  const admin = createServerClient();

  // Age-gate 18+ (CRIT-10): chi genera deve essere adulto verificato. Gli account
  // nati prima del gate non hanno la data: 403 dedicato -> il client chiede la DOB
  // una volta sola. I minorenni (difesa) ricevono un blocco secco.
  const { data: callerProfile } = await admin
    .from("profiles")
    .select("date_of_birth, adult_verified_at")
    .eq("id", user.id)
    .maybeSingle();
  const ageReason = adultGateReason(callerProfile, new Date());
  if (ageReason) {
    logBlockedRequest(admin, { source: "generate", reason: ageReason === "no_dob" ? "age_no_dob" : "age_under_18", category });
    if (ageReason === "no_dob") {
      return NextResponse.json({ error: "Conferma la tua data di nascita per continuare.", code: "age_unverified" }, { status: 403 });
    }
    return NextResponse.json({ error: "SEMBLIC e' riservato ai maggiorenni." }, { status: 403 });
  }
```

(Il blocco va inserito subito dopo `const admin = createServerClient();` alla riga 69; il fetch avatar e i veti restano sotto, invariati.)

- [ ] **Step 3: Type-check**

Run: `node node_modules/typescript/bin/tsc --noEmit`
Expected: PASS.

- [ ] **Step 4: Verifica manuale del comportamento (smoke)**

Avvia `npm run dev`, login con `test-card-e3@h2ai.dev`. Con un profilo senza `adult_verified_at` (account di test pre-gate), una POST a `/api/generate` deve tornare 403 con `code: "age_unverified"`. Dopo aver valorizzato `adult_verified_at` (via confirm-age, Task 8), deve passare.

- [ ] **Step 5: Commit**

```bash
git add app/api/generate/route.ts
git commit -m "Age-gate: gate eta sul chiamante in /api/generate

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>"
```

---

## Task 7: Campo DOB e schermata di blocco al signup (`app/signup/SignupForm.tsx`)

**Files:**
- Modify: `app/signup/SignupForm.tsx`

Riusa `isAdult` da `lib/age.ts` per il check client-side (UX immediata). Il trigger (Task 3) resta la garanzia. UI a tre tendine GG / Mese / AAAA come da mockup approvato, inserita dopo Password e prima del tipo account. Copy senza trattini lunghi.

- [ ] **Step 1: Aggiungi import e stato**

In cima, aggiungi all'import:
```typescript
import { isAdult } from "@/lib/age";
```
Dentro il componente, accanto agli altri `useState`:
```typescript
  const [dobDay, setDobDay] = useState("");
  const [dobMonth, setDobMonth] = useState("");
  const [dobYear, setDobYear] = useState("");
  const [blockedUnderage, setBlockedUnderage] = useState(false);
```

- [ ] **Step 2: Costruisci la DOB e valida l'età in `handleSubmit`**

All'inizio di `handleSubmit`, dopo `setLoading(true)` e prima del check password, aggiungi:
```typescript
    // Data di nascita autodichiarata: la componiamo come YYYY-MM-DD.
    if (!dobDay || !dobMonth || !dobYear) {
      setError("Inserisci la tua data di nascita");
      setLoading(false);
      return;
    }
    const dob = `${dobYear}-${dobMonth.padStart(2, "0")}-${dobDay.padStart(2, "0")}`;
    if (!isAdult(dob, new Date())) {
      // Strato 1 (UX): schermata di blocco. Il trigger e' la garanzia dietro.
      setBlockedUnderage(true);
      setLoading(false);
      return;
    }
```

E passa la DOB nel metadata del signUp (in `options.data`):
```typescript
      options: { data: { full_name: fullName, role, date_of_birth: dob, ...(isEnterprise ? { account_intent: "enterprise" } : {}) } },
```

- [ ] **Step 3: Aggiungi la schermata di blocco e il campo DOB nel render**

Subito dentro `<Shell ...>`, prima del `done ? (...)`, gestisci il blocco:
```tsx
      {blockedUnderage ? (
        <div style={{ textAlign: "center" }}>
          <p style={{ color: "var(--blocked-c)", fontWeight: 700, fontSize: "1rem", margin: "0 0 0.6rem" }}>
            SEMBLIC e' riservato ai maggiorenni
          </p>
          <p style={{ color: "var(--text-muted)", fontSize: "0.85rem", lineHeight: 1.6, margin: 0 }}>
            In base alla data di nascita inserita non possiamo creare il tuo account. SEMBLIC custodisce volti di persone reali e l'accesso e' consentito solo dai 18 anni.
          </p>
        </div>
      ) : done ? (
```
(chiudi la parentesi del ternario esistente di conseguenza: diventa `blockedUnderage ? (...) : done ? (...) : (form)`.)

Nel form, tra il campo Password e il blocco "Tipo di account", inserisci le tre tendine:
```tsx
          <div>
            <label style={labelStyle}>Data di nascita</label>
            <div style={{ display: "flex", gap: "0.4rem" }}>
              <select value={dobDay} onChange={(e) => setDobDay(e.target.value)} required
                style={{ flex: "0 0 64px", padding: "0.7rem 0.5rem", borderRadius: 10, background: "var(--surface)", border: "1px solid var(--hairline-soft)", color: "var(--text)", fontSize: "0.85rem" }}>
                <option value="">GG</option>
                {Array.from({ length: 31 }, (_, i) => i + 1).map((d) => <option key={d} value={String(d)}>{d}</option>)}
              </select>
              <select value={dobMonth} onChange={(e) => setDobMonth(e.target.value)} required
                style={{ flex: 1, padding: "0.7rem 0.5rem", borderRadius: 10, background: "var(--surface)", border: "1px solid var(--hairline-soft)", color: "var(--text)", fontSize: "0.85rem" }}>
                <option value="">Mese</option>
                {["Gennaio","Febbraio","Marzo","Aprile","Maggio","Giugno","Luglio","Agosto","Settembre","Ottobre","Novembre","Dicembre"].map((name, i) => <option key={name} value={String(i + 1)}>{name}</option>)}
              </select>
              <select value={dobYear} onChange={(e) => setDobYear(e.target.value)} required
                style={{ flex: "0 0 90px", padding: "0.7rem 0.5rem", borderRadius: 10, background: "var(--surface)", border: "1px solid var(--hairline-soft)", color: "var(--text)", fontSize: "0.85rem" }}>
                <option value="">AAAA</option>
                {Array.from({ length: 100 }, (_, i) => new Date().getFullYear() - i).map((y) => <option key={y} value={String(y)}>{y}</option>)}
              </select>
            </div>
            <p style={{ color: "var(--verified-c)", fontSize: "0.72rem", margin: "0.45rem 0 0", lineHeight: 1.5 }}>
              Devi avere almeno 18 anni per usare SEMBLIC.
            </p>
          </div>
```

- [ ] **Step 4: Type-check + verifica mobile/desktop**

Run: `node node_modules/typescript/bin/tsc --noEmit`
Expected: PASS.
Avvia `npm run dev`, apri `/signup`. Verifica con `preview_resize` (mobile 375 e desktop) che le tre tendine non sforino in larghezza e che la micro-copy stia su una riga pulita. Inserendo una data under-18 e premendo "Crea account" deve comparire la schermata di blocco senza chiamare il signup.

- [ ] **Step 5: Commit**

```bash
git add app/signup/SignupForm.tsx
git commit -m "Age-gate: campo data di nascita e schermata di blocco al signup

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>"
```

---

## Task 8: Route conferma DOB una-tantum (`app/api/account/confirm-age/route.ts`)

**Files:**
- Create: `app/api/account/confirm-age/route.ts`

- [ ] **Step 1: Crea la route**

```typescript
// app/api/account/confirm-age/route.ts
// Prompt una-tantum per gli account nati prima del gate: l'utente conferma la
// data di nascita. Validazione autoritativa lato server (fail-closed).
import { NextResponse } from "next/server";
import { createAuthClient } from "@/lib/supabase-auth";
import { createServerClient } from "@/lib/supabase";
import { isAdult } from "@/lib/age";

export const runtime = "nodejs";

export async function POST(request: Request) {
  const auth = await createAuthClient();
  const { data: { user } } = await auth.auth.getUser();
  if (!user) return NextResponse.json({ error: "Devi accedere" }, { status: 401 });

  const body = await request.json().catch(() => null);
  const dob = String(body?.date_of_birth ?? "").trim(); // atteso "YYYY-MM-DD"

  if (!isAdult(dob, new Date())) {
    return NextResponse.json({ error: "Per usare SEMBLIC devi avere almeno 18 anni." }, { status: 403 });
  }

  const admin = createServerClient();
  await admin
    .from("profiles")
    .update({ date_of_birth: dob, adult_verified_at: new Date().toISOString(), adult_verified_method: "self" })
    .eq("id", user.id);

  return NextResponse.json({ ok: true });
}
```

- [ ] **Step 2: Type-check**

Run: `node node_modules/typescript/bin/tsc --noEmit`
Expected: PASS.

- [ ] **Step 3: Commit**

```bash
git add app/api/account/confirm-age/route.ts
git commit -m "Age-gate: route conferma data di nascita una-tantum

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>"
```

---

## Task 9: Modale prompt DOB nella UI di generazione

**Files:**
- Create: `app/match/AgeGateModal.tsx`
- Modify: `app/match/MatchClient.tsx`

- [ ] **Step 1: Crea il componente modale**

```tsx
// app/match/AgeGateModal.tsx
"use client";

import { useState } from "react";

// Prompt una-tantum: appare quando /api/generate risponde 403 code "age_unverified".
// Posta a /api/account/confirm-age; in caso di successo chiama onConfirmed().
export function AgeGateModal({ onClose, onConfirmed }: { onClose: () => void; onConfirmed: () => void }) {
  const [day, setDay] = useState("");
  const [month, setMonth] = useState("");
  const [year, setYear] = useState("");
  const [error, setError] = useState<string | null>(null);
  const [loading, setLoading] = useState(false);

  async function submit() {
    if (!day || !month || !year) { setError("Inserisci la tua data di nascita"); return; }
    setLoading(true);
    setError(null);
    const date_of_birth = `${year}-${month.padStart(2, "0")}-${day.padStart(2, "0")}`;
    const res = await fetch("/api/account/confirm-age", {
      method: "POST",
      headers: { "Content-Type": "application/json" },
      body: JSON.stringify({ date_of_birth }),
    });
    const json = await res.json().catch(() => ({}));
    setLoading(false);
    if (!res.ok) { setError(json.error ?? "Errore"); return; }
    onConfirmed();
  }

  const sel: React.CSSProperties = { padding: "0.7rem 0.5rem", borderRadius: 10, background: "var(--surface)", border: "1px solid var(--hairline-soft)", color: "var(--text)", fontSize: "0.85rem" };

  return (
    <div style={{ position: "fixed", inset: 0, zIndex: 60, background: "rgba(8,10,16,0.7)", display: "flex", alignItems: "center", justifyContent: "center", padding: "1rem" }}>
      <div className="glass" style={{ width: "100%", maxWidth: 380, borderRadius: 18, padding: "1.4rem 1.2rem" }}>
        <p style={{ color: "#F2A93B", fontSize: "0.65rem", letterSpacing: "0.16em", textTransform: "uppercase", margin: "0 0 0.5rem" }}>Una verifica veloce</p>
        <h3 style={{ fontWeight: 300, letterSpacing: "-0.02em", fontSize: "1.3rem", margin: "0 0 0.5rem" }}>Prima di continuare</h3>
        <p style={{ color: "var(--text-muted)", fontSize: "0.82rem", lineHeight: 1.6, margin: "0 0 1rem" }}>
          Per legge dobbiamo sapere che hai almeno 18 anni. Inserisci la tua data di nascita: la chiediamo una volta sola.
        </p>
        <div style={{ display: "flex", gap: "0.4rem" }}>
          <select value={day} onChange={(e) => setDay(e.target.value)} style={{ ...sel, flex: "0 0 64px" }}>
            <option value="">GG</option>
            {Array.from({ length: 31 }, (_, i) => i + 1).map((d) => <option key={d} value={String(d)}>{d}</option>)}
          </select>
          <select value={month} onChange={(e) => setMonth(e.target.value)} style={{ ...sel, flex: 1 }}>
            <option value="">Mese</option>
            {["Gennaio","Febbraio","Marzo","Aprile","Maggio","Giugno","Luglio","Agosto","Settembre","Ottobre","Novembre","Dicembre"].map((name, i) => <option key={name} value={String(i + 1)}>{name}</option>)}
          </select>
          <select value={year} onChange={(e) => setYear(e.target.value)} style={{ ...sel, flex: "0 0 90px" }}>
            <option value="">AAAA</option>
            {Array.from({ length: 100 }, (_, i) => new Date().getFullYear() - i).map((y) => <option key={y} value={String(y)}>{y}</option>)}
          </select>
        </div>
        {error && <p style={{ color: "var(--blocked-c)", fontSize: "0.78rem", margin: "0.7rem 0 0" }}>{error}</p>}
        <button onClick={submit} disabled={loading} style={{ marginTop: "1rem", width: "100%", background: "#F2A93B", color: "#412402", border: "none", borderRadius: 999, padding: "0.8rem", fontWeight: 700, fontSize: "0.9rem", cursor: loading ? "default" : "pointer" }}>
          {loading ? "Verifica…" : "Conferma e continua"}
        </button>
        <p style={{ color: "var(--text-muted)", fontSize: "0.7rem", margin: "0.7rem 0 0", lineHeight: 1.5 }}>
          Serve solo a verificare l'eta. Resta privata, non viene mostrata nel tuo profilo.
        </p>
        <button onClick={onClose} style={{ marginTop: "0.6rem", width: "100%", background: "transparent", color: "var(--text-muted)", border: "none", fontSize: "0.78rem", cursor: "pointer" }}>
          Annulla
        </button>
      </div>
    </div>
  );
}
```

- [ ] **Step 2: Aggancia il modale in `app/match/MatchClient.tsx`**

Aggiungi l'import:
```typescript
import { AgeGateModal } from "./AgeGateModal";
```
Aggiungi lo stato accanto agli altri `useState`:
```typescript
  const [ageGate, setAgeGate] = useState(false);
```
Nel gestore della risposta di `/api/generate` (dopo il blocco 402 `volt`, prima del generico `if (!res.ok)`), inserisci:
```typescript
        if (res.status === 403 && json.code === "age_unverified") {
          setGeneratingHandle(null);
          setAgeGate(true);
          return;
        }
```
Nel JSX, rendi il modale quando `ageGate` e' true (ad es. vicino agli altri overlay come `voltGate`):
```tsx
      {ageGate && (
        <AgeGateModal
          onClose={() => setAgeGate(false)}
          onConfirmed={() => setAgeGate(false)}
        />
      )}
```

- [ ] **Step 3: Type-check + verifica visiva mobile/desktop**

Run: `node node_modules/typescript/bin/tsc --noEmit`
Expected: PASS.
Con `npm run dev`, simula un 403 `age_unverified` (profilo senza `adult_verified_at`) e verifica con `preview_resize` che il modale sia centrato e leggibile a 375 e desktop, e che dopo "Conferma e continua" si chiuda.

- [ ] **Step 4: Commit**

```bash
git add app/match/AgeGateModal.tsx app/match/MatchClient.tsx
git commit -m "Age-gate: modale prompt data di nascita una-tantum nella generazione

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>"
```

---

## Task 10: Strato forte sulla creazione avatar (`app/api/avatar/create/route.ts`)

**Files:**
- Modify: `app/api/avatar/create/route.ts`

Nel registro entra solo un volto di adulto provato dal documento. Dopo il Task 5, chi e' KYC-approved con eta >= 18 ha `adult_verified_method = 'document'`; chi non lo e' (DOB illeggibile, revisione manuale) viene fermato qui.

- [ ] **Step 1: Estendi la select del profilo del chiamante**

La route carica gia' il profilo (circa righe 79-84) con `select("role, kyc_status, full_name")`. Estendilo:
```typescript
const { data: profile } = await auth
  .from("profiles")
  .select("role, kyc_status, full_name, adult_verified_method")
  .eq("id", user.id)
  .single();
```

- [ ] **Step 2: Aggiungi il gate adulto-documentale dopo il check identita-volto (riga ~216)**

Subito dopo il blocco `if (idCheck && idCheck.band === "weak") { ... }`:
```typescript
  // Age-gate 18+ (strato forte): un volto entra nel registro solo se l'utente e'
  // adulto provato dal documento. Chi non ha l'esito documentale (under-18
  // rifiutato, o DOB illeggibile in revisione) non passa.
  if (profile?.adult_verified_method !== "document") {
    return NextResponse.json(
      { error: "Per registrare il tuo volto devi completare la verifica d'identita (18+)." },
      { status: 403 },
    );
  }
```

- [ ] **Step 3: Type-check**

Run: `node node_modules/typescript/bin/tsc --noEmit`
Expected: PASS.

- [ ] **Step 4: Commit**

```bash
git add app/api/avatar/create/route.ts
git commit -m "Age-gate: creazione avatar solo per adulti verificati da documento

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>"
```

> **Nota per Morelz (decisione separata, non in questo task):** la registrazione "sola protezione" (`/api/veto/register`) protegge un volto dalla generazione, non lo rende generabile. Bloccare i minori dalla protezione e' un dilemma (proteggere un minore dai deepfake e' un bene), e tocca anche se trattare biometrici di minori. Per questo NON aggiungo qui un gate alla protezione: lo lasciamo come scelta a parte. Dopo il Task 5 un minore KYC-approved non esiste comunque (rifiutato all'approvazione).

---

## Task 11: Back-fill dei 12 founder (gated: serve la lista di Morelz)

**Files:** nessuno (operazione dati via `execute_sql`).

I founder sono avatar reali con KYC e consenso firmato: vanno segnati adulti verificati da documento, ma NON alla cieca.

- [ ] **Step 1: Identifica i candidati e mostrali a Morelz**

Esegui via `execute_sql` (sola lettura):
```sql
select p.id, p.email, p.full_name, p.role, p.kyc_status, a.handle, a.alias
from profiles p
join avatars a on a.owner = p.id   -- verificare il nome reale della FK owner
where p.adult_verified_at is null
order by a.alias;
```
Presenta l'elenco a Morelz e fatti confermare quali sono i 12 founder.

- [ ] **Step 2: Back-fill solo gli ID confermati (gated: "applica")**

Con la lista confermata, esegui:
```sql
update profiles
set adult_verified_at = now(), adult_verified_method = 'document'
where id in ( /* ID confermati da Morelz */ );
```
Expected: numero di righe = numero di founder confermati.

- [ ] **Step 3: Verifica**

```sql
select count(*) from profiles where adult_verified_method = 'document';
```
Expected: >= numero di founder back-fillati.

---

## Task 12: Verifica finale completa

**Files:** nessuno.

- [ ] **Step 1: Suite test completa**

Run: `npm test`
Expected: tutti verdi (i ~97 esistenti + i nuovi di age/adult-gate/didit).

- [ ] **Step 2: Type-check**

Run: `node node_modules/typescript/bin/tsc --noEmit`
Expected: 0 errori.

- [ ] **Step 3: Build**

Run: `npm run build`
Expected: build verde.

- [ ] **Step 4: Smoke manuale (dev)**

Con `npm run dev`:
1. `/signup` con data under-18 → schermata di blocco, nessun account creato.
2. `/signup` con data >= 18 → account creato, profilo con `date_of_birth` + `adult_verified_method='self'`.
3. Generazione con account pre-gate (senza `adult_verified_at`) → modale DOB; conferma >= 18 → procede; conferma under-18 → blocco.
4. Verifica mobile (375) e desktop di signup e modale con `preview_resize`.

- [ ] **Step 5: Riepilogo a Morelz**

Riassumi cosa e' pronto, cosa resta gated (applicazione migrazione se non ancora fatta, back-fill founder, conferma campo DOB Didit su decisione reale), e attendi "pubblica" per push + deploy.

---

## Self-Review (svolta in fase di scrittura)

- **Copertura spec:** strato 1 (Task 3 trigger + Task 7 signup + Task 6/8/9 gate a valle e prompt), strato 2 (Task 4 estrazione + Task 5 webhook), dati (Task 3), UI (Task 7/9 + blocco in Task 7), osservabilita (Task 5), test (Task 1/2/4), privacy/minimizzazione (mai salvata la DOB documentale, Task 5), non-goal rispettati (niente interstitial d'ingresso). Founder back-fill (Task 11) e gate avatar (Task 10) coperti.
- **Placeholder:** nessuno nel codice; le uniche parti "da confermare" (lista founder, nome FK `avatars.owner`, campo DOB Didit) sono esplicitate come step con dipendenza esterna, non come codice mancante.
- **Coerenza tipi:** `ageFromDob`/`isAdult`/`kycAdultOutcome` (lib/age) usati coerentemente; `adultGateReason` firma `(profile, now)` ovunque; `extractDobFromDecision`/`getDiditDob` coerenti tra didit.ts e webhook; `code: "age_unverified"` coerente tra generate route e MatchClient.
